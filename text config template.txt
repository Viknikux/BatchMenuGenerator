// SYNTAX: title [Your Menu Title Here] (Max 57 characters)
// EXAMPLE: title Admin Toolbox
// This field is required
title [your title here]

// SYNTAX: description [Text line]
// Note: You can repeat this keyword on new lines to add multiple description paragraphs.
// EXAMPLE: description Version 1.0 - Use with administrator privileges.
// This field is not required, simply dont add the description line to not include
description 

// SYNTAX: pass [Password]
// Note: Leave blank, or use 'n' or 'no' if you do not want a password.
// EXAMPLE: pass MySecurePass123
// This field is not required
pass [your password here, type n, no or dont include this line to not use a password]

// SYNTAX: admin [y or n]
// Note: Type 'admin y' to force the script to request administrative privileges upon launch.
// If not typed, or set to 'n', it runs with standard privileges.
admin y/n

// CRITICAL: You MUST define this total number BEFORE writing individual option details!
// SYNTAX: option num: [Integer from 1 to 20]
// EXAMPLE: option num: 2
// This field is required
option num: [the number of options you want to have]

// Repeat the structure below for every option up to your allocated total.

// --- OPTION 1 ---
// SYNTAX: option [Number] label [Menu Display Name]
// EXAMPLE: option 1 label Check System Information
// This is required for every option up to the limit you set
option 1 label [your label]

// SYNTAX: option [Number] { [Your Batch Commands] }
// Note: Put every batch command on its own separate line inside the brackets.
option 1 {
    [commands go here inside the brackets, in separate lines]
}
// SYNTAX: runmode [0 or 1]
// CRITICAL: Must be placed immediately after the closing bracket '}'
// 0 = Direct Call (call your_command)
// 1 = CMD Wrapper (cmd /c "your_command")
// This field is required
runmode [for direct cmd commands, chose 0, for .exe files with their respective paths, chose 1]


// --- OPTION 2 ---
option 2 label [your label]
option 2 { 
    [your commands]
}
runmode [0 or 1]

// SYNTAX OPTIONS FOR 'save':
//   save n                         -> Preview the script only (will not write to disk)
//   save --[filename.bat]          -> Auto-saves to the folder where this tool runs [eg your downloads folder]
//   save --[filename.bat]--[path]  -> Auto-saves to a specific target folder path [you have to type it in ofc, if the folder doesent exist the tool will automatically create the folders]
// EXAMPLE: save --my_new_batch_menu.bat--C:\Users\User\Desktop
save [your perfered option]